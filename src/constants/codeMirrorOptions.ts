export default {
  mode: 'gfm',
  theme: 'base16-light',
  lineNumbers: false,
  lineWrapping: true,
  styleActiveLine: { nonEmpty: true },
}

export enum Folders {
  ALL = 'ALL',
  TRASH = 'TRASH',
  FAVORITES = 'FAVORITES',
  CATEGORY = 'CATEGORY',
}